# ETF Tracker 專案交接文件

> 最後更新：2026-04-24（遷移至 Cloudflare Pages；00981A 經理人卡片圖 + lightbox；持股更新排程改為每 30 分鐘/共 10 次；00988A 獨立 workflow；prevTotalShares 修正為只與前一個交易日比較）

---

## 專案簡介

追蹤十檔主動式 ETF 的每日持股變化，自動更新靜態網頁並推送 Telegram 通知。完全無需本地伺服器，所有自動化均由 GitHub Actions 在雲端執行。

- **網站**：https://woody-etf-tracker.pages.dev （Cloudflare Pages 託管）
- **GitHub 倉庫**：https://github.com/WuminWu/woody-etf-tracker
- **本地目錄**：`d:\Self_Tools\ETF_Tracker\`
- **GitHub Pages**：已停用（原 `wuminwu.github.io/woody-etf-tracker` 已關閉）

---

## 追蹤 ETF 清單

| 代號 | 名稱 | 經理人 | 資料來源類型 |
|------|------|--------|-------------|
| 00981A | 統一台股增長 | 陳釧瑤 | A：ezmoney.com.tw（Playwright + XLSX 下載）|
| 00988A | 主動統一全球創新 | 陳意婷 | A：ezmoney.com.tw（Playwright + XLSX 下載，包含海外股轉換）|
| 00980A | 野村臺灣智慧優選 | 游景德 | B：Nomura JSON API |
| 00985A | 野村台灣50 | 林浩詳 | B：Nomura JSON API |
| 00991A | 復華未來50 | 呂宏宇 | C：fhtrust.com.tw 直接 GET |
| 00992A | 群益科技創新 | 陳朝政 | D：capitalfund.com.tw（Playwright + Angular）|
| 00982A | 群益台灣強棒 | 陳沅易 | D：capitalfund.com.tw（Playwright + Angular）|
| 00987A | 台新台灣優勢成長 | 魏永祥 | E：tsit.com.tw（靜態 HTML 解析，無需 Playwright）|
| 00993A | 安聯台灣 | （每日由 API 取得）| F：etf.allianzgi.com.tw（Playwright 攔截 XHR）|
| 00995A | 中信台灣卓越 | （每日由 API 取得）| G：ctbcinvestments.com.tw（plain HTTP + AuthToken）|

---

## 目錄結構

```
ETF_Tracker/
├── index.html                       # 前端主頁
├── app.js                           # 前端邏輯
├── style.css                        # 樣式
├── favicon.svg                      # 網站圖示
├── requirements.txt                 # Python 套件清單
├── .env                             # Telegram 憑證（不進 git）
├── data_00981A.json                 # 持股資料（GitHub Actions 每日自動更新）
├── data_00980A.json
├── data_00985A.json
├── data_00991A.json
├── data_00992A.json
├── data_00982A.json
├── data_00987A.json
├── data_00993A.json
├── data_00995A.json
├── data_00988A.json
├── data_index.json                  # 加權指數 YTD（check_and_update_index.py 每日更新）
├── check_and_update_00981A.py       # 00981A 每日更新腳本
├── check_and_update_00980A.py
├── check_and_update_00985A.py
├── check_and_update_00991A.py
├── check_and_update_00992A.py
├── check_and_update_00982A.py
├── check_and_update_00987A.py
├── check_and_update_00993A.py
├── check_and_update_00995A.py
├── check_and_update_00988A.py
├── check_and_update_index.py        # 每日抓取加權指數 ^TWII YTD，產生 data_index.json
├── update_prices.py                 # 每日收盤後更新 ETF 股價與 YTD（不更動持股）
├── sheets_helper.py                 # Google Sheets 整合模組（所有腳本共用）
├── check_managers.py                # 每月檢查經理人是否異動的獨立腳本
├── force_sync_to_sheets.py          # 手動強制同步目前 JSON 至 Sheets 的腳本
├── manager_00981A.jpg               # 00981A 經理人卡片圖（網頁 Header 顯示）
├── .github/
│   └── workflows/
│       ├── daily_update.yml         # 每日持股更新（台股9檔，15:00–19:30 台灣時間，每30分鐘）
│       ├── daily_update_00988A.yml  # 00988A 專屬更新（06:00–10:30 台灣時間，週二–六，每30分鐘）
│       ├── price_update.yml         # 每日股價更新排程（收盤後）
│       ├── check_managers.yml       # 每月1日檢查經理人異動排程
│       └── force_sync.yml           # 手動觸發的 Sheets 批次寫入排程
├── holdings/                        # 歷史持股快照（每日一檔）
│   ├── 00981A_holdings_YYYY-MM-DD.json
│   ├── 00980A_holdings_YYYY-MM-DD.json
│   └── ...（其餘同理）
└── 交接文件.md
```

---

## 網頁功能

### 1. 今年以來績效排名（YTD）
- 位於 Header 下方、分頁按鈕上方
- 同時讀取所有 10 檔 ETF 的 data JSON，依 YTD 由高到低排序
- 前三名顯示金/銀/銅漸層圓形獎牌（含彩色光暈），其餘顯示灰色圓形序號
- 點擊任一列可直接切換至該 ETF 的持股清單，同步高亮顯示選中項
- 標題下方顯示「加權指數績效 +x.xx%」（紅色正值、綠色負值）

### 2. 持股清單（Holdings Tab）
- **00981A 經理人卡片圖**：選取 00981A 時，Header ETF 名稱右側顯示經理人卡片圖（`manager_00981A.jpg`）；點擊可全螢幕放大（lightbox），按 ESC 或點擊背景關閉；切換其他 ETF 時自動隱藏
- ETF 選擇器（下拉選單）切換標的
- 顯示欄位：序號、股票代號/名稱、股價（含日期）、股數（前後對比格式）、持股比例（含前後比較箭頭）、狀態（新增/出清/加碼/減碼）、加減碼股數、加減碼金額
- **欄位說明**
  - **股價**：最新股價，後方以灰色小字顯示資料日期（如 `12.34 (2026-04-22)`）
  - **股數**：若有變動，以 `灰色舊值 → 彩色新值` 格式顯示；無變動僅顯示當前值
  - **比例**：同股數格式，`灰色舊值 → 彩色新值`
- **排序功能**：
  - 加減碼金額：點擊欄位標題，循環切換 降冪 → 升冪 → 預設（按 todayWeight）
  - 加減碼股數：同上
  - 狀態：按 新增(4) > 加碼(3) > 持平(2) > 減碼(1) > 出清(0) 優先序排序
- Header 顯示：ETF 名稱、經理人、ETF 當日股價（含更新日期）、YTD 績效
- 顯示資料日期（最新交易日）及最後更新時間

### 3. ETF 持股交叉比對（Cross Tab）
- 彙整所有 10 檔 ETF 持股，找出被 **2 檔以上** ETF 同時持有的標的
- 顯示：序號、股票代號/名稱、持有的 ETF 列表（依持股比例由高到低排序顯示）、持有 ETF 數量
- 持有比例數字特別以紅色顯眼標示，方便快速比對
- 持有 ETF 數欄可點擊排序（降冪/升冪）
- 切換至此 Tab 時，Header 的「基金規模/市值」資訊自動隱藏

### 4. 股票搜尋（Stock Search Tab）
- 採用左右雙欄 (Split View) 的動態介面（手機版自動轉為上下堆疊）。
- 提供即時搜尋功能，在左側輸入股票代號（如 `2330`）或名稱（如 `台積電`），右側會即時過濾顯示。
- 顯示：持有該股票的 ETF 清單及其權重佔比，並依照權重由高至低自動排序。
- 效能優化：完全在前端記憶體 (`globalStockMap`) 即時處理比對，搜尋過程無任何網路延遲。

### 5. 響應式設計 (RWD)
- **手機版卡片式介面**：在寬度小於 `768px` 的裝置上，所有持股清單、交叉比對表格與搜尋結果會自動轉換為「卡片式排列 (Card Layout)」，提升手機用戶閱讀體驗，無須左右滑動即可檢視所有欄位。

---

## 自動化機制（GitHub Actions）

### Workflow 1：daily_update.yml（台股 9 檔持股更新）

**觸發時間（台灣時間，週一至週五，每 30 分鐘，共 10 次）**

| UTC | 台灣時間 |
|-----|---------|
| 07:00–11:30 | 15:00–19:30 |

cron：`0,30 7-11 * * 1-5`

**執行流程：**
1. checkout 最新 repo
2. 安裝 Python 3.11 + pip 套件 + Playwright Chromium
3. 依序執行 9 支腳本（不含 00988A，每支腳本自行判斷是否需要更新）
4. 執行 `check_and_update_index.py`（更新加權指數 YTD → `data_index.json`）
5. `git add data_*.json holdings/` → commit → push（Cloudflare 偵測 push 後自動部署）

- 若持股已更新：腳本直接跳過，不重複通知
- 若持股尚未更新：發送 Telegram 等待訊息，30 分鐘後再試
- **重要**：各腳本內部已**移除** `git_push()` 呼叫，git commit/push 統一由此 workflow 最後一步執行

### Workflow 1b：daily_update_00988A.yml（00988A 專屬更新）

**觸發時間（台灣時間，週二至週六，每 30 分鐘，共 10 次）**

| UTC | 台灣時間 | 說明 |
|-----|---------|------|
| 前一天 22:00–23:30 | 06:00–07:30 | cron: `0,30 22-23 * * 1-5` |
| 00:00–02:30 | 08:00–10:30 | cron: `0,30 0-2 * * 2-6` |

> **原因**：00988A 持股涵蓋美股，需等美股收盤（台灣時間凌晨）後隔天早上才有完整資料。

**執行流程：**
1. 僅執行 `check_and_update_00988A.py`
2. `git add data_00988A.json holdings/` → commit → push

### Workflow 2：price_update.yml（股價更新）

每日收盤後（台灣時間約下午 3–4 點）執行 `update_prices.py`：
- 更新所有 10 檔 ETF 的 `etfPrice`、`ytd`、`priceDate`（不更動持股資料）
- 更新 `data_index.json`（加權指數 ^TWII YTD）
- **注意**：此 workflow **不更動** `lastUpdate`，避免與持股更新時間混淆

### Workflow 3：check_managers.yml（經理人異動監控）
**觸發時間**：每月 1 日 00:00 (UTC)
**執行流程**：
執行 `check_managers.py`，以 Playwright 或 urllib 前往各家 ETF 官網搜尋目前寫死的經理人姓名。
若發現網頁上找不到原本的經理人名字，則判定為可能已更換經理人，會自動發送 Telegram 警告提醒手動更新程式碼。此獨立腳本設計可避免因抓取名字失敗而影響每日最重要的持股更新。

### Workflow 4：force_sync.yml（手動強制同步 Sheets）
**觸發時間**：手動觸發 (`workflow_dispatch`)
**執行流程**：
執行 `python force_sync_to_sheets.py --dedup`，自動讀取所有 `data_{CODE}.json`，從每個 JSON 的 `meta.dataDate` 取得正確日期，將持股批次寫入 Google Sheets（已存在的記錄會自動跳過）。寫入完成後執行 dedup 清理重複行。可安全重複執行，適合起點建立或緊急補漏。

---

## 各腳本更新邏輯

### 通用流程（所有腳本）
1. 計算前一交易日（跳過週末）
2. 若 `holdings/` 已存在該日快照 → **直接結束**（不重複抓取）
3. 向來源網站抓取今日持股（**同步取得官方 AUM/發行單位數**，見下方 AUM 來源說明）
4. 若持股資料為空 → 發送 Telegram 等待通知後結束（等下次重試）
5. 儲存持股快照至 `holdings/{CODE}_holdings_{date}.json`
6. 讀取最近一筆歷史快照作為比較基準
7. 用 yfinance 取得每檔股票當日收盤價（包含針對 00988A 等海外成分股，自動將 US/JP/KR/HK 等各國國家代碼後綴轉換成正確的 Yahoo Finance `.T`, `.KS`, `.HK` 等後綴，台股則預設先試 `.TW`，失敗再試 `.TWO`）
8. 計算差異（股數、金額）、YTD、ETF 股價
9. 產生 `data_{CODE}.json`
10. 呼叫 `sheets_helper.append_holdings_to_sheets(etf_code, data_date, holdings, meta=wrapper["meta"])` 將當日持股（含基金市值/股數）寫入 Google Sheets
11. 發送 Telegram 成功通知（含加碼/減碼/新增/出清明細）
12. **git push 已移除**：不在腳本內 push，由 daily_update.yml 最後統一執行 `git add data_*.json holdings/` → commit → push

### 各腳本資料來源說明

> **AUM 資料原則**：所有腳本已改為從官方網站取得 AUM（基金規模）與發行單位數，**不再使用 yfinance**（yfinance 對台灣 ETF 常回傳 0 或過期數值）。

#### 類型 A：ezmoney.com.tw（00981A、00988A）
- Playwright 開啟網頁，點擊下載 XLSX
- 驗證 XLSX 民國日期欄位是否符合前一交易日
- 從第 19 行起解析持股
- 00988A 含海外成分股（US/JP/KR/HK），腳本自動轉換 Yahoo Finance 後綴（`.T`、`.KS`、`.HK` 等）
- **AUM**：`parse_aum_from_xlsx()` 掃描 XLSX 表頭，搜尋「淨資產」和「流通在外單位數」對應欄位（row 0–15 內），直接從同一 XLSX 取得
- **注意**：XLSX 表頭格式偶發異常（如欄位位移），若 prevTotalShares 寫入異常值，需手動修正 data_{CODE}.json

#### 類型 B：Nomura JSON API（00980A、00985A）
- `POST https://www.nomurafunds.com.tw/API/ETFAPI/api/Fund/GetFundAssets`
- Payload：`{"FundID": "00980A", "SearchDate": "YYYY-MM-DD"}`
- 純 HTTP 請求，無需 Playwright
- **AUM**：從同一 API response 中取 `result["Entries"]["Data"]["FundAsset"]["Aum"]` 及 `["Units"]`

#### 類型 C：fhtrust.com.tw（00991A）
- `GET https://www.fhtrust.com.tw/api/assetsExcel/ETF23/{YYYYMMDD}`
- 直接下載 XLSX，無需 Playwright
- **AUM**：`parse_aum_from_xlsx()` 讀取 XLSX（`header=None`），搜尋「基金資產淨值」標籤，取其下一行的數值

#### 類型 D：capitalfund.com.tw（00992A、00982A）
- Playwright 開啟 Angular 頁面
- 必須用 JS `HTMLInputElement.prototype.value` setter + `dispatchEvent` 設定日期（Angular 雙向綁定，不能用 Playwright `.type()`）
- **日期偏移**：填入 run_date（當天），實際取得前一交易日資料（已在腳本中處理）
- 產品 ID：00992A = 500，00982A = 399
- **AUM**：`parse_aum_from_xlsx()` 讀取 Sheet 0（「投資組合」），搜尋「基金淨資產價值」與「已發行受益權單位總數」標籤

#### 類型 E：tsit.com.tw（00987A）
- `GET https://www.tsit.com.tw/ETF/Home/ETFSeriesDetail/00987A`
- 純靜態 HTML，urllib 直接抓取，無需 Playwright
- 持股表格中股票代碼含 ` TT` 後綴（如 `2330 TT`），解析時自動去除
- 用 `re.fullmatch(r'\d{4}', code)` 辨識有效股票代碼列
- **AUM**：`fetch_aum_from_html()` 以相同 URL 再次請求，regex 搜尋「基金淨資產價值(元)」與「已發行受益權單位總數」

#### 類型 F：etf.allianzgi.com.tw（00993A）
- 目標：`https://etf.allianzgi.com.tw/etf-info/E0002?tab=4`
- Playwright 開啟頁面，**攔截 XHR 回應**（`page.on("response", handler)`）
- 先瀏覽 `tab=1` 取得 `GetFundDetail`（含經理人姓名），再瀏覽 `tab=4` 取得 `GetFundAssets`（含持股）
- Allianz API 有 CSRF 保護，直接 HTTP 請求會得到 400；必須在 Playwright 瀏覽器 session 內攔截
- 解析：`tables[1]["Rows"]` = 持股列表，`tables[2]["Rows"]` = 基金規模
- 資料日期：`PCFDate` 欄位（Allianz 慣例：PCFDate 為次一交易日，`dataDate` 直接使用此值）
- **AUM**：直接從 `GetFundAssets` response 中解析（與持股同一個 API）

#### 類型 G：ctbcinvestments.com.tw（00995A）
- 無需 Playwright，純 HTTP 三步驟：
  1. `POST /API/home/AuthToken` → 取得 `token`
  2. `POST /API/etf/ETFDetail?token=...` → 取得經理人姓名
  3. `POST /API/etf/ETFHoldingWeight?token=...` → 取得持股資料（`FID: "E0036"`）
- Bootstrap 邏輯：若無歷史快照，自動從 CTBC 補抓前一交易日資料
- **AUM**：從 `FundAssets[0]` 取最大兩個逗號分隔整數（直接來自官方 API）

---

## Google Sheets 整合

每日持股資料在寫入 `data_{CODE}.json` 後，會自動追加至 Google Sheets，方便長期歷史分析。

### 試算表資訊

| 項目 | 說明 |
|------|------|
| 試算表網址 | https://docs.google.com/spreadsheets/d/1oK8ICXl0euyxJocRxHLknzWD6bccukKoWQI_ke_XqA8 |
| Sheet 名稱 | `holdings` |

### 欄位定義

| 欄位 | 內容 | 範例 |
|------|------|------|
| A 日期 | YYYY-MM-DD | `2026-04-22` |
| B ETF代號 | ETF 代碼 | `00981A` |
| C 股票代號 | 個股代碼 | `2330` |
| D 股票名稱 | 個股名稱 | `台積電` |
| E 股數 | 持股股數（股） | `1000000` |
| F 持股比例(%) | 當日持股比例 | `28.5` |
| G 前日比例(%) | 前一交易日比例 | `27.9` |
| H 股價 | 當日收盤價（元） | `950.0` |
| I 加減碼股數 | 正加碼、負減碼 | `20000` |
| J 加減碼金額 | 正加碼、負減碼（元） | `19000000.0` |
| K 基金市值(億) | 基金當日總市值，同一 ETF+日期所有行相同 | `17.53` |
| L 基金股數(張) | 基金流通在外單位數（張），同一 ETF+日期所有行相同 | `7532` |

> **注意**：`todayWeight <= 0`（已出清）的持倉不寫入 Sheets，節省空間。K/L 欄為基金層級資料，可用來追蹤 ETF 規模成長曲線。

### 核心模組：sheets_helper.py

```python
# 公開 API（各腳本呼叫）
append_holdings_to_sheets(etf_code, data_date, holdings, meta=None)
# etf_code  : str        e.g. "00981A"
# data_date : str        e.g. "2026-04-22"
# holdings  : list       wrapper["holdings"] 中的清單
# meta      : dict|None  wrapper["meta"]，用於填入 K 基金市值 / L 基金股數欄位；
#                        傳 None 時這兩欄留白（向下相容）

# 去重工具（force_sync 或手動呼叫）
delete_duplicate_rows()
# 掃描整張 Sheet，以 (日期, ETF代號, 股票代號) 為 key，
# 保留第一筆、刪除後續重複行，從最後一行往上刪以保持 index 正確。
# 回傳刪除筆數。
```

- **重複保護**：寫入前先比對 A:B 欄（日期 + ETF代號），若已存在則跳過
- **dedup 清理**：`delete_duplicate_rows()` 可在 force_sync 後或手動呼叫，以 (日期+ETF代號+股票代號) 為 key 去除完全重複行
- **非阻塞**：Sheets API 失敗只 log warning，不影響持股 JSON 寫入或 Telegram 通知
- **認證**：透過環境變數 `GOOGLE_SHEETS_CREDENTIALS`（Service Account JSON）

### 設定步驟（新環境部署）

1. **Google Cloud Console** → 建立專案 → 啟用 Google Sheets API
2. **IAM & Admin → Service Accounts** → 建立 Service Account → 下載 JSON key
3. **Google Sheet** → 右上角「共用」→ 輸入 Service Account 的 email（`xxxxx@project.iam.gserviceaccount.com`）→ 給予「編輯者」權限
4. **GitHub** → Settings → Secrets → Repository secrets → 新增 `GOOGLE_SHEETS_CREDENTIALS`，值為 JSON key 的全文（整段貼上）

---

## data_{CODE}.json 格式

```json
{
  "meta": {
    "manager": "魏永祥",
    "ytd": "5.23",
    "etfPrice": 12.34,
    "priceDate": "2026-04-22",
    "dataDate": "2026-04-21",
    "lastUpdate": "2026-04-21 16:15",
    "totalShares": 12345,
    "prevTotalShares": 12300,
    "totalMarketCap": 15.23,
    "prevTotalMarketCap": 15.18
  },
  "holdings": [
    {
      "rank": 1,
      "code": "2330",
      "name": "台積電",
      "shares": 1000000,
      "prevShares": 980000,
      "price": 950.00,
      "yestWeight": 28.5,
      "todayWeight": 29.1,
      "diffShares": 20000,
      "diffAmount": 19000000.0
    }
  ]
}
```

**欄位說明（新增欄位）：**

| 欄位 | 說明 |
|------|------|
| `priceDate` | 股價更新日期（由 `update_prices.py` / `price_update.yml` 寫入） |
| `lastUpdate` | 持股資料最後更新時間（僅由各 `check_and_update_*.py` 寫入） |
| `prevTotalShares` | **前一個交易日**的基金規模（張數）；若前一交易日無資料（腳本跳日）則為 0，網頁不顯示變化箭頭 |
| `prevTotalMarketCap` | **前一個交易日**的總市值（億元）；同上邏輯 |

---

## data_index.json 格式

```json
{
  "twii_ytd": "12.34",
  "lastUpdate": "2026-04-22 15:30"
}
```

由 `check_and_update_index.py` 在 `daily_update.yml` 最後一步執行，以 yfinance 抓取 `^TWII` YTD（period="ytd"）並寫入此檔。供網頁 YTD 排名列標題下方顯示加權指數績效。

---

## Telegram 通知設定

### 本地開發
憑證存於 `ETF_Tracker/.env`（已加入 `.gitignore`，不會上傳 git）：

```
TELEGRAM_BOT_TOKEN=你的BotToken
TELEGRAM_CHAT_ID=你的ChatID
```

### GitHub Actions
至 GitHub 倉庫 → **Settings → Secrets and variables → Actions → Repository secrets** 新增：

| Secret 名稱 | 說明 |
|------------|------|
| `TELEGRAM_BOT_TOKEN` | Telegram Bot API Token |
| `TELEGRAM_CHAT_ID` | 接收通知的 Chat ID |
| `GOOGLE_SHEETS_CREDENTIALS` | Google Service Account JSON（整段貼入，見下方 Google Sheets 章節） |

> **注意**：必須設在 Repository secrets，不是 Environment secrets（Workflow 未指定 environment，Environment secrets 無法讀取）。

### 通知訊息格式

**持股更新成功：**
```
📊 00987A 台新台灣優勢成長 持股更新
📅 資料日期：2026-04-21
💰 ETF 股價：12.34　　YTD：+5.23%
📦 持股數量：30 檔

🔴 加碼：3 檔　🟢 減碼：2 檔
🟣 新增：1 檔　🟠 出清：0 檔
...
```

**持股尚未更新（等待重試）：**
```
⏳ 00987A 台新台灣優勢成長 持股尚未更新
📅 資料日期：2026-04-21
🔄 將於下一個小時再次檢查...
```

---

## 環境設定（新環境部署）

### Python 套件安裝

```bash
pip install -r requirements.txt
python -m playwright install chromium
```

`requirements.txt` 內容：
```
playwright>=1.42.0
yfinance>=0.2.37
pandas>=2.0.0
openpyxl>=3.1.0
python-dotenv>=1.0.0
google-api-python-client>=2.100.0
google-auth>=2.23.0
```

### 本地手動執行（測試用）

```bash
cd d:\Self_Tools\ETF_Tracker
python check_and_update_00987A.py
```

---

## 新增 ETF 步驟

1. **確認資料來源類型**（A/B/C/D/E/F/G，見上方說明），選最相近的腳本複製
2. **複製腳本**：`check_and_update_{新代號}.py`
3. 修改腳本頂部常數：`ETF_CODE`、`MANAGER`、API URL 或 Product ID
4. **index.html**：在 `<select id="etf-selector">` 新增 `<option value="{代號}">`
5. **app.js**：在 `ALL_ETFS` 和 `ETF_LIST` 兩個陣列中各新增 `{ id: '代號', name: '名稱' }`
6. **update_prices.py**：在 `ETFS` 清單中新增 `("{代號}", "data_{代號}.json")`
7. **daily_update.yml**：在執行步驟新增一行 `python check_and_update_{代號}.py`（若為海外股 ETF，改放入 `daily_update_00988A.yml` 或新建專屬 workflow）
8. 執行腳本產生初始 `data_{CODE}.json`
9. Git push → Cloudflare Pages 約 15–30 秒後自動部署生效

---

## 常見問題

| 問題 | 原因 | 解法 |
|------|------|------|
| Angular 日期選擇器填值無效 | `.type()` 不觸發 Angular 雙向綁定 | 用 JS prototype setter + `dispatchEvent` |
| capitalfund 資料日期偏移一天 | 網站選 T 日返回 T-1 資料 | 腳本已處理，勿調整日期邏輯 |
| Telegram 400 Bad Request | 股票名稱含 HTML 特殊字元 | 勿使用 `parse_mode=HTML` |
| yfinance 股價為 0.0 | 未上市、休市或盤中（尚未收盤）| 正常現象，收盤後自動補正；依序試 `.TW`、`.TWO` |
| 網站未更新 | push 後 Cloudflare 尚未部署 | 等待 15–30 秒；至 Cloudflare Dashboard 確認部署狀態 |
| Telegram 通知未收到 | Secrets 設在 Environment 而非 Repository | 移至 Repository secrets（Settings → Secrets → Actions）|
| GitHub Actions Node.js 警告 | actions 尚未原生支援 Node 24 | Workflow 已設 `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true` |
| tsit.com.tw 解析到 0 筆持股 | HTML 結構異動或解析器過早停止 | 確認 `HoldingsTableParser` 掃描全部 `<tr>`，勿限制表格編號 |
| Allianz GetFundAssets 回傳 400 | CSRF 保護，直接 HTTP 請求被拒絕 | 必須在 Playwright session 內攔截 XHR，不可改用純 HTTP |
| CTBC Playwright headless 被擋 | WAF 偵測到 headless 瀏覽器 | 改用純 HTTP + AuthToken 流程（無需 Playwright）|
| lastUpdate 顯示股價更新時間 | update_prices.py 誤更動 lastUpdate | 確認 update_prices.py 只寫 priceDate，不寫 lastUpdate |
| 網頁股價日期與持股日期不同 | 兩者由不同 workflow 更新 | 正常：priceDate 由 price_update.yml 更新，dataDate 由 daily_update.yml 更新 |
| Sheets 未寫入 | GOOGLE_SHEETS_CREDENTIALS 未設 / Service Account 未共用試算表 | 檢查 GitHub Secrets 是否為完整 JSON；確認 Sheet 已分享給 Service Account email（編輯者） |
| Sheets 重複寫入同一天 | 腳本重複執行或 force_sync 執行多次 | sheets_helper 寫入前已比對 A:B（日期+ETF代號）防重；若已重複，執行 `delete_duplicate_rows()` 或觸發 force_sync.yml 可自動清除 |
| 00981A 基金規模顯示 0 | yfinance 回傳 0（已修復）或 JSON 殘留舊零值 | 已改用官方 XLSX；若 JSON 仍為 0，手動填入正確值（可從 ezmoney XLSX 表頭讀取），下次執行後自動正確 |
| AUM 與官網不符 | 腳本仍使用 yfinance | 檢查對應腳本是否有 parse_aum_from_xlsx 或 fetch_aum_from_html，若無請補上 |
| 基金規模/市值顯示翻倍異常 | XLSX 解析異常導致 prevTotalShares 寫入錯誤值 | 已修正：腳本現在驗證前一天 dataDate，只有前一個交易日才做比較；若仍異常，手動修正 data_{CODE}.json 的 prevTotalShares |
| 基金規模變化顯示空白（無箭頭）| 腳本跳日（前一交易日無資料），prevTotalShares 設為 0 | 正常現象，隔天腳本正常執行後恢復顯示 |
| 只有第一支腳本的資料出現在 GitHub Pages | 各腳本內部 git_push() 造成後續腳本 push 被 reject（non-fast-forward）| 確認各腳本已移除 git_push() 呼叫，統一由 daily_update.yml 最後一步 push；若舊腳本仍有 git_push()，刪除該函數呼叫即可 |
